%h2 Class06 Answer:
%h2 Operate iris_model in JavaScript.

%p I operated iris_model in JavaScript using my Chrome-browser.

%p I got started by calling a simple shell script to start a web server:

.syntax
  %pre
    %code.bash
      cd ~ann/mljs
      \./http.bash

%p Next, I browsed a URL: http://localhost:8080/iris2.html

%p I saw this:
%img(src='/class06/iris22.png')
%hr/

%p Asking the model to issue a prediction is easy; I just pressed the predict-button.

%p To see JavaScript create the model, I right-clicked on the page to bring up the inspect-UI:

%img(src='/class06/iris24.png')
%hr/

%p I found the HTML which connects the button to JavaScript:

%img(src='/class06/iris25.png')
%hr/

%p I found handleClick() in iris2.js:

%img(src='/class06/iris26.png')
%hr/

%p I set a breakpoint at line 32:

%img(src='/class06/iris27.png')
%hr/



%p
  %a(href='class06#iris')
    Class06 Lab
