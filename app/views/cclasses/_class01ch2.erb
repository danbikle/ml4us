<code class='bash'>
cd ~
mkdir -p Downloads bin
cd Downloads
wget http://chromedriver.storage.googleapis.com/2.23/chromedriver_linux64.zip
unzip chromedriver_linux64.zip
cp -p chromedriver ~/bin
echo 'export PATH=${HOME}/bin:$PATH' >> ~/.bashrc
bash
chromedriver --help
</code>
