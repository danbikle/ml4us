%h2 Class04 Answer:
%h2 Test your installation of Java.

%p
  To test my installation of Java, I first try this shell command:

.syntax
  %pre
    %code.bash
      which java

%p I should see something like this:
.syntax
  %pre
    %code.bash
      dan@pavlap:~ $ 
      dan@pavlap:~ $ 
      dan@pavlap:~ $ which java
      \/home/dan/jdk/bin/java
      dan@pavlap:~ $ 
      dan@pavlap:~ $

%p Next, I will try actually running a simple Java command:

.syntax
  %pre
    %code.bash
      java -version
      
%p I should see something like this:
.syntax
  %pre
    %code.bash
      dan@pavlap:~ $ 
      dan@pavlap:~ $ java -version
      java version "1.8.0_102"
      Java(TM) SE Runtime Environment (build 1.8.0_102-b14)
      Java HotSpot(TM) 64-Bit Server VM (build 25.102-b14, mixed mode)
      dan@pavlap:~ $ 
      dan@pavlap:~ $
